---
kernelspec:
  name: book-venv
  display_name: Python 3 (book)
---

# 职责边界

> 学完本节，你能回答：一条“上传音频”请求在后端经历了哪些职责？为什么要把 API 设计、业务逻辑、数据流转、安全防护拆开，而不是写在一个函数里？

## 后端不是“写接口”，而是“守边界”

如果把前端比作餐厅的前厅，后端就是后厨与库房——前厅负责呈现与交互，后厨负责按单生产、核验食材、管理库存、保证出品安全。把所有事塞进一个大函数，就像让一个人同时点菜、炒菜、管账、看门，迟早在高峰期崩溃。

后端的四条职责线可以这样切分：

- **API 设计**：对外契约。做什么（资源与动作）、怎么叫（路径与方法）、传什么（请求/响应形状）、错了怎么说（状态码与错误体）。契约一旦发布就是承诺，改动成本高，故需先设计后实现。
- **业务逻辑**：领域规则。MeetingToText 中“创建任务 → 切片 → 转写 → 生成纪要”的编排、状态机流转、去重与幂等，都属于业务层。业务层不该知道 HTTP 细节，只关心“给定输入，产出什么”。
- **数据流转**：持久化与传输。从请求体到领域对象、从领域对象到数据库行、从数据库到响应体的映射，以及事务边界与一致性。`m2t.store.TaskStore` 就是数据流转的最简形态。
- **安全防护**：守门。认证（你是谁）、授权（你能做什么）、校验（输入是否合法）、限流与审计。安全不是末尾的补丁，而是贯穿每一层的检查点。

四者的理想流向是：`HTTP 契约 → 校验 → 业务编排 → 持久化 → 响应`，安全检查散落在入口、边界与落库前。

## 反例：为什么不写“大函数”

```python
# 反例示意（不推荐）：一个函数包揽所有职责
def handle_upload(request):
    # 1) 手动解析 JSON 2) 手写 if 校验 3) 直接拼 SQL 4) 无统一错误格式
    pass
```

问题在于**变更耦合**：改校验规则要动 SQL，改表结构要动路由，测试必须端到端，难以独立回归。分层后，每层可独立测试与替换——改校验不碰存储，换数据库不改路由。

> **环境约定**：本书面向 Linux，本节示例在所有平台行为一致；若涉及文件路径，统一用 `pathlib.Path` 处理路径。

## 可运行示例：最小后端职责边界（FastAPI 端点示例）

示例（单文件演示四层如何各司其职，用 `TestClient` 本地验证，无网络）：

```{code-cell} ipython3
import tempfile, pathlib
from typing import Annotated

from fastapi import FastAPI, Header, HTTPException, Depends
from fastapi.testclient import TestClient
from pydantic import BaseModel, Field
from m2t.store import TaskStore

# --- 数据流转层：复用教学包的 TaskStore（持久化边界） ---
tmpdir = tempfile.TemporaryDirectory()
db_path = pathlib.Path(tmpdir.name) / "boundary.db"
store = TaskStore(db_path)

# --- 业务逻辑层：纯函数/类，不依赖 FastAPI ---
class TaskService:
    def __init__(self, store: TaskStore):
        self.store = store

    def create_task(self, task_id: str, filename: str) -> dict:
        # 业务规则：task_id 与 filename 必为非空；重复 id 视为冲突
        if not task_id.strip() or not filename.strip():
            raise ValueError("task_id and filename must be non-empty")
        if self.store.get(task_id) is not None:
            raise ValueError(f"task {task_id} already exists")
        self.store.create(task_id, filename, full_text="")
        row = self.store.get(task_id)
        assert row is not None
        return row

service = TaskService(store)

# --- API 契约层：Pydantic 定义输入/输出形状 ---
class CreateTaskIn(BaseModel):
    task_id: str = Field(min_length=1, description="任务 ID")
    filename: str = Field(min_length=1, description="原始文件名")

class CreateTaskOut(BaseModel):
    id: str
    filename: str
    status: str

# --- 安全防护层：极简 API Key 校验（演示边界，不做真实鉴权） ---
def verify_api_key(x_api_key: Annotated[str | None, Header()] = None):
    # 教学演示：若携带则必须为 demo-secret，否则放行（便于对比“有/无防护”的差异）
    if x_api_key is not None and x_api_key != "demo-secret":
        raise HTTPException(status_code=401, detail="invalid api key")
    return x_api_key

app = FastAPI(title="MeetingToText Boundary Demo")

@app.post("/tasks", response_model=CreateTaskOut, dependencies=[])
def create_task(payload: CreateTaskIn, api_key: str | None = Depends(verify_api_key)):
    try:
        row = service.create_task(payload.task_id, payload.filename)
    except ValueError as e:
        msg = str(e)
        if "already exists" in msg:
            raise HTTPException(status_code=409, detail=msg)
        raise HTTPException(status_code=422, detail=msg)
    return CreateTaskOut(id=row["id"], filename=row["filename"], status=row["status"])

@app.get("/tasks/{task_id}", response_model=CreateTaskOut)
def get_task(task_id: str):
    row = store.get(task_id)
    if row is None:
        raise HTTPException(status_code=404, detail="task not found")
    return CreateTaskOut(id=row["id"], filename=row["filename"], status=row["status"])

# --- 本地验证：用 TestClient 演示“契约 → 业务 → 持久化 → 防护”的分层效果 ---
client = TestClient(app)

# 1) 正常创建
r1 = client.post("/tasks", json={"task_id": "t1", "filename": "meeting.wav"})
print("create t1:", r1.status_code, r1.json())
assert r1.status_code == 200
assert r1.json()["id"] == "t1"

# 2) 重复创建 → 409（业务层冲突转化为 HTTP 语义）
r2 = client.post("/tasks", json={"task_id": "t1", "filename": "meeting.wav"})
print("duplicate:", r2.status_code, r2.json()["detail"])
assert r2.status_code == 409

# 3) 非法输入 → 422（Pydantic 契约层拦截，未进入业务层）
r3 = client.post("/tasks", json={"task_id": "", "filename": "x.wav"})
print("empty id:", r3.status_code)
assert r3.status_code == 422

# 4) 安全层：错误 api key → 401
r4 = client.post("/tasks", json={"task_id": "t2", "filename": "a.wav"}, headers={"x-api-key": "bad"})
print("bad key:", r4.status_code, r4.json()["detail"])
assert r4.status_code == 401

# 5) 正确 api key 放行
r5 = client.post("/tasks", json={"task_id": "t2", "filename": "a.wav"}, headers={"x-api-key": "demo-secret"})
print("good key:", r5.status_code, r5.json())
assert r5.status_code == 200

# 6) 查询
r6 = client.get("/tasks/t2")
print("get t2:", r6.json())
assert r6.json()["filename"] == "a.wav"

tmpdir.cleanup()
print("边界校验通过：契约/业务/持久化/防护各司其职")
# 预期输出:
# create t1: 200 {'id': 't1', ...}
# duplicate: 409 task t1 already exists
# empty id: 422
# bad key: 401 invalid api key
# good key: 200 {'id': 't2', ...}
# get t2: {'id': 't2', ...}
# 边界校验通过：契约/业务/持久化/防护各司其职
```

```bash
# 启动服务
uvicorn demo_boundary:app --reload --port 8000
# 验证（本地 TestClient 已覆盖，无需真实网络）
.venv/bin/python -m pytest --collect-only 2>&1 | head -n 20
```

> **与后续章节的衔接**：本节的“契约—业务—持久化—防护”四分法将在 [第4章 HTTP 与 RESTful](../../part2_backend_development/chapter04_http_restful/index.md) 细化为路由与状态码，在 [第5章 数据持久化](../../part2_backend_development/chapter05_persistence_sql_orm/index.md) 展开为事务与 ORM，在 [第10章 健壮性与安全](../../part4_advanced_engineering/chapter10_robustness_security/index.md) 回到认证与校验的纵深防御。

```
