---
kernelspec:
  name: book-venv
  display_name: Python 3 (book)
---

# RESTful 成熟度模型

> 学完本节，你能回答：为什么“所有操作都 POST 到 `/api/action`”难以演进？四个阶梯各自解决了什么问题，又引入了什么代价？MeetingToText 的 API 处在哪一级，是否有必要再往上走？

## 先动手：把一个 RPC 接口重构成 Level 2

先不背定义，直接对比同一业务在两个水位的返回差异。下面的 Demo 用三套 `FastAPI` 应用模拟同一任务创建，`TestClient` 会打印 `status_code` 与 `Location` 的分野，差异本身就是成熟度的定义。

```{code-cell} ipython3
import tempfile, pathlib
from fastapi import FastAPI, HTTPException, Response, status
from fastapi.testclient import TestClient
from pydantic import BaseModel, Field
from m2t.store import TaskStore

tmpdir = tempfile.TemporaryDirectory()
db_path = pathlib.Path(tmpdir.name) / "maturity.db"
store = TaskStore(db_path)

class TaskCreateIn(BaseModel):
    task_id: str = Field(min_length=1)
    filename: str = Field(min_length=1)

# Level 0：单一入口，靠 Body 的 action 区分
app_l0 = FastAPI(title="Level 0 - RPC")
class RpcIn(BaseModel):
    action: str
    task_id: str | None = None
    filename: str | None = None

@app_l0.post("/api")
def rpc_dispatch(payload: RpcIn):
    if payload.action == "createTask":
        if not payload.task_id or not payload.filename:
            raise HTTPException(status_code=400, detail="missing fields")
        if store.get(payload.task_id) is not None:
            raise HTTPException(status_code=409, detail="already exists")
        store.create(payload.task_id, payload.filename)
        return {"ok": True, "id": payload.task_id}
    elif payload.action == "getTask":
        if not payload.task_id:
            raise HTTPException(status_code=400, detail="missing task_id")
        row = store.get(payload.task_id)
        if row is None:
            raise HTTPException(status_code=404, detail="not found")
        return {"ok": True, "task": row}
    else:
        raise HTTPException(status_code=400, detail=f"unknown action {payload.action}")

# Level 2：资源加动词加状态码
app_l2 = FastAPI(title="Level 2 - HTTP Verbs + Status Codes")
class TaskOut(BaseModel):
    id: str
    filename: str
    status: str

@app_l2.post("/tasks", response_model=TaskOut, status_code=status.HTTP_201_CREATED)
def l2_create(payload: TaskCreateIn, response: Response):
    if store.get(payload.task_id) is not None:
        raise HTTPException(status_code=409, detail="already exists")
    store.create(payload.task_id, payload.filename)
    row = store.get(payload.task_id)
    assert row is not None
    response.headers["Location"] = f"/tasks/{payload.task_id}"
    return TaskOut(id=row["id"], filename=row["filename"], status=row["status"])

@app_l2.get("/tasks/{task_id}", response_model=TaskOut)
def l2_get(task_id: str):
    row = store.get(task_id)
    if row is None:
        raise HTTPException(status_code=404, detail="not found")
    return TaskOut(id=row["id"], filename=row["filename"], status=row["status"])

@app_l2.delete("/tasks/{task_id}", status_code=status.HTTP_204_NO_CONTENT)
def l2_delete(task_id: str):
    row = store.get(task_id)
    if row is None:
        return Response(status_code=status.HTTP_204_NO_CONTENT)
    store.delete(task_id)
    return Response(status_code=status.HTTP_204_NO_CONTENT)

@app_l2.get("/tasks/{task_id}/hyper", response_model=dict)
def l2_hyper(task_id: str):
    row = store.get(task_id)
    if row is None:
        raise HTTPException(status_code=404, detail="not found")
    return {
        "id": row["id"],
        "filename": row["filename"],
        "status": row["status"],
        "links": {
            "self": f"/tasks/{task_id}",
            "transcribe": f"/tasks/{task_id}/transcribe",
            "export": f"/tasks/{task_id}/export",
        },
    }

# 观测：Level 0 往往全返回 200，语义藏在 Body
c0 = TestClient(app_l0)
r0 = c0.post("/api", json={"action": "createTask", "task_id": "m0", "filename": "a.wav"})
print("L0 create:", r0.status_code, r0.json())
assert r0.status_code == 200
r0b = c0.post("/api", json={"action": "getTask", "task_id": "m0"})
print("L0 get:", r0b.json()["task"]["id"])
assert r0b.json()["task"]["id"] == "m0"

# 观测：Level 2 用状态码与 Location 自描述
c2 = TestClient(app_l2)
r2 = c2.post("/tasks", json={"task_id": "m2", "filename": "c.wav"})
print("L2 create:", r2.status_code, r2.headers.get("location"))
assert r2.status_code == 201
assert r2.headers["location"] == "/tasks/m2"
r2b = c2.get("/tasks/m2")
print("L2 get:", r2b.json())
assert r2b.json()["id"] == "m2"
r2c = c2.get("/tasks/m2/hyper")
print("L3 hyper links:", r2c.json()["links"])
assert "self" in r2c.json()["links"]
r2d = c2.post("/tasks", json={"task_id": "m2", "filename": "c.wav"})
print("L2 duplicate:", r2d.status_code)
assert r2d.status_code == 409
r2e = c2.delete("/tasks/m2")
print("L2 delete:", r2e.status_code)
assert r2e.status_code == 204

tmpdir.cleanup()
print("对照校验通过：L0 与 L2 的语义与代价可见")
# 预期输出:
# L0 create: 200 {'ok': True, ...}
# L0 get: m0
# L2 create: 201 /tasks/m2
# L2 get: {'id': 'm2', ...}
# L3 hyper links: {'self': ...}
# L2 duplicate: 409
# L2 delete: 204
# 对照校验通过：...
```

关键观测只有两行：Level 0 创建成功只给 `200` 加 `{"ok": true}`，调用方必须读 Body 才知道含义，网关与缓存无法利用它。Level 2 创建成功给 `201 + Location`，重复给 `409`，删除给 `204`，基础设施与前端可直接按状态码分支。

## 一段话说清演进：从单入口到自描述

Richardson 把上面的差异抽象为四级约束的叠加。Level 0 是单一入口加单一方法，多为 `POST`，靠 Body 区分动作，类似 `POST /api/action {op: "createTask"}`。Level 1 按资源拆分 URL，出现 `POST /tasks` 与 `GET /tasks/{id}`。Level 2 再叠加 HTTP 动词与状态码，让缓存对 `GET` 生效、重试对 `PUT` 与 `DELETE` 安全、网关可按状态码限流，这也是本书推荐的默认水位。Level 3 再叠加超媒体，在响应中携带下一步可执行的链接，客户端通过 `links` 自发现能力，适合需向第三方暴露可演进能力的开放 API。

是否往上走取决于协作模式。MeetingToText 前后端同团队，由 `openapi.json` 生成类型与客户端，URL 的可发现性已由契约生成器解决，Level 2 已覆盖绝大多数需求，强行做超媒体会增加解析与文档成本。

## 四级的代价与收益

* Level 0 到 1：收益是资源可寻址，代价是需梳理领域名词如 Task 与 Transcript，避免动词堆砌
* Level 1 到 2：收益是语义可被基础设施理解，代价是团队需统一状态码与幂等约定
* Level 2 到 3：收益是可演进与自描述，客户端无需硬编码 URL，代价是前后端都要理解并消费超媒体，调试与测试成本上升，在强契约闭环中边际收益递减

## URL 风格对照：同一业务的写法差异

* Level 0：`POST /api` 加 `{"action":"createTask","filename":"a.wav"}`，所有操作挤在单一端点
* Level 1：`POST /tasks` 与 `GET /tasks/t1` 与 `POST /tasks/t1/transcribe`，资源已拆分但状态码可能仍混用
* Level 2：`POST /tasks` 返回 `201 + Location`，`GET /tasks/{id}` 返回 `200` 或 `404`，`DELETE /tasks/{id}` 返回 `204`
* Level 3：在 Level 2 响应体中追加 `links`，客户端据此决定下一步，而非硬编码 URL 模板

> 环境约定：本书面向 Linux，URL 设计与平台无关，若涉及文件路径统一用 `pathlib.Path` 处理。

## 可运行对照：资源拆分后的状态码精细化

Level 1 到 Level 2 的精细化也可用最小代码自证。下面复用同一 `TaskStore`，对比 `400` 与 `409` 的分野：

```{code-cell} ipython3
import tempfile, pathlib
from fastapi import FastAPI, HTTPException
from fastapi.testclient import TestClient
from pydantic import BaseModel, Field
from m2t.store import TaskStore

tmpdir = tempfile.TemporaryDirectory()
db_path = pathlib.Path(tmpdir.name) / "maturity2.db"
store = TaskStore(db_path)

class TaskCreateIn(BaseModel):
    task_id: str = Field(min_length=1)
    filename: str = Field(min_length=1)

# Level 1：已按资源拆分，但冲突仍用 400 表达
app_l1 = FastAPI(title="Level 1 - Resources")
@app_l1.post("/tasks")
def l1_create(payload: TaskCreateIn):
    if store.get(payload.task_id) is not None:
        raise HTTPException(status_code=400, detail="already exists")
    store.create(payload.task_id, payload.filename)
    return {"id": payload.task_id, "status": "pending"}

@app_l1.post("/tasks/{task_id}/transcribe")
def l1_transcribe(task_id: str):
    if store.get(task_id) is None:
        raise HTTPException(status_code=404, detail="not found")
    return {"id": task_id, "transcribed": True}

c1 = TestClient(app_l1)
r1 = c1.post("/tasks", json={"task_id": "m1", "filename": "b.wav"})
print("L1 create:", r1.status_code, r1.json())
assert r1.status_code == 200
r1b = c1.post("/tasks/m1/transcribe")
print("L1 transcribe:", r1b.json())
assert r1b.json()["transcribed"] is True
# 同一冲突在 Level 2 会被表达为 409，调用方可据此区分“参数错”与“资源已存在”
print("L1 观测完成：资源已拆分，但状态码仍可更精确")

tmpdir.cleanup()
```

```bash
# 本地验证
.venv/bin/python -c "from fastapi.testclient import TestClient; print('maturity demo ok')"
```

> 与 MeetingToText 的衔接：`backend/app/routers` 的现有设计（`upload` 与 `record` 与 `transcribe` 等）本质是 Level 1 的按资源拆分，叠加 [4.1 的状态码语义](4.1_http_protocol_essence.md) 后即达到 Level 2，是否引入超媒体取决于是否需向第三方提供自发现能力，详见 [4.5 OpenAPI 契约](4.5_openapi_contract_test.md)。
