---
kernelspec:
  name: book-venv
  display_name: Python 3 (book)
---

# HTTP 协议精髓

> 学完本节，你能回答：为什么 `GET` 与 `POST` 不能互换？幂等性对重试与网关意味着什么？看到 `200 / 201 / 204 / 400 / 404 / 409 / 422` 时，你能否立刻判断“谁该为这个错误负责”？Header 又如何承载内容协商、缓存与追踪？

## 方法不是动词大全，而是语义契约

HTTP 方法（Method）的本质是**对资源的操作语义**，而非“能发请求的几种方式”。类比餐厅点单：`GET` 是“看菜单”（只读、可反复看），`POST` 是“下新单”（创建、每次都可能产生新菜品），`PUT` 是“整单替换”（幂等地把单子重写一遍），`PATCH` 是“改一个菜”（局部更新），`DELETE` 是“撤单”。

关键分水岭是**安全性**与**幂等性**：

| 方法 | 是否安全（不改资源） | 是否幂等（重复执行效果一致） | 典型用途 |
|------|---------------------|---------------------------|----------|
| GET | 是 | 是 | 查询任务、拉取列表 |
| HEAD | 是 | 是 | 只取 Header 探活 |
| POST | 否 | 否 | 创建任务、触发转写 |
| PUT | 否 | 是 | 全量替换任务元数据 |
| PATCH | 否 | 不一定* | 修改任务状态/文本片段 |
| DELETE | 否 | 是 | 删除任务 |

> `PATCH` 的幂等性取决于实现：若语义是“设为 completed”则幂等，若是“计数 +1”则非幂等——契约由你定义，而非方法名自动保证。

幂等性之所以重要，是因为**网络不可靠**：超时后客户端是否可安全重试，取决于该方法是否幂等。网关、重试中间件与前端的“重试按钮”都依赖这一语义做决策。

## 状态码是机器可读的“责任划分”

状态码的首位数字即是一次粗分：

- `2xx` 成功：`200 OK`（通用成功）、`201 Created`（资源已创建，宜带 `Location`）、`204 No Content`（成功但无返回体，常见于 `DELETE`）
- `3xx` 重定向：`301 / 302` 永久/临时跳转、`304 Not Modified` 配合缓存
- `4xx` 客户端错：`400` 通用坏请求、`401` 未认证、`403` 已认证但无权限、`404` 资源不存在、`409` 资源冲突（重复创建）、`422` 语义不可处理（校验失败，FastAPI/Pydantic 的首选）、`413` 载荷过大、`429` 限流
- `5xx` 服务端错：`500` 未捕获异常、`502 / 503 / 504` 网关/过载/超时

MeetingToText 的映射可作为参考：上传格式错误 → `400`，空文件或魔数不符 → `400`，重复任务 → `409`，校验失败 → `422`，文件过大 → `413`，任务不存在 → `404`，鉴权失败 → `401`，服务端未捕获 → `500`。状态码不是装饰，而是**调用方决定“重试、提示用户、或报警”**的依据。

> **选型中立性**：`400` vs `422` 的争论由来已久。本书遵循 FastAPI/Pydantic 约定：语法层面无法解析用 `400`，语义层面校验失败用 `422`，二者在 `4xx` 范畴内均合理，关键是团队统一并在契约中可预期。

## Header：元数据的“信封”

如果说 URL 与 Body 是信件正文，Header 就是信封上的标记——承载**内容协商**（`Content-Type / Accept`）、**缓存**（`Cache-Control / ETag / If-None-Match`）、**认证与追踪**（`Authorization / X-Request-Id`）、**跨域与安全**（`CORS / Content-Security-Policy`）等正交关注点。

两个常被忽视的实践：

1. **内容协商**：`Accept: application/json` 明确期望 JSON，而非靠后缀 `?format=json`。
2. **请求追踪**：网关或前端注入 `X-Request-Id`，后端原样回传并写入日志，跨服务排障时以此串联调用链。MeetingToText 的 `backend/app/server.py` 在 CORS 与日志中即预留了此类扩展点。

> **环境约定**：本书面向 Linux，Header 键名大小写不敏感，但值大小写敏感；涉及文件上传的 `Content-Type: multipart/form-data` 边界由浏览器/客户端自动生成，手写易错，依赖框架的 `UploadFile` 抽象即可。

## 可运行示例：用 FastAPI + TestClient 演示 HTTP 语义

文件 `book/part2_backend_development/chapter04_http_restful/demo_http_semantics.py`（单文件演示方法、幂等性、状态码与 Header，无网络）：

```{code-cell} ipython3
import tempfile, pathlib

from fastapi import FastAPI, Header, HTTPException, Response, status
from fastapi.testclient import TestClient
from pydantic import BaseModel, Field
from m2t.store import TaskStore

# --- 持久化：复用 m2t.store.TaskStore（与第3章一致，展示契约与存储的分离） ---
tmpdir = tempfile.TemporaryDirectory()
db_path = pathlib.Path(tmpdir.name) / "http_semantics.db"
store = TaskStore(db_path)

# --- 契约：Pydantic 模型（输入校验 + 输出形状） ---
class TaskCreateIn(BaseModel):
    task_id: str = Field(min_length=1, max_length=64, description="任务 ID")
    filename: str = Field(min_length=1, description="文件名")

class TaskOut(BaseModel):
    id: str
    filename: str
    status: str

app = FastAPI(title="HTTP Semantics Demo")

# 计数器：演示幂等性差异（PUT 覆盖 vs POST 累加在真实业务中的体现）
put_counter = {"count": 0}

@app.get("/tasks/{task_id}", response_model=TaskOut)
def get_task(task_id: str, x_request_id: str | None = Header(default=None)):
    # 回显 X-Request-Id（若调用方提供），演示 Header 作为追踪信封
    row = store.get(task_id)
    if row is None:
        raise HTTPException(status_code=404, detail="task not found")
    return TaskOut(id=row["id"], filename=row["filename"], status=row["status"])

@app.post("/tasks", response_model=TaskOut, status_code=status.HTTP_201_CREATED)
def create_task(payload: TaskCreateIn, response: Response):
    # POST 非幂等：重复创建同一 id 视为冲突（409），而非静默覆盖
    if store.get(payload.task_id) is not None:
        raise HTTPException(status_code=409, detail=f"task {payload.task_id} already exists")
    store.create(payload.task_id, payload.filename)
    row = store.get(payload.task_id)
    assert row is not None
    # 201 Created 时设置 Location，指向新资源
    response.headers["Location"] = f"/tasks/{payload.task_id}"
    return TaskOut(id=row["id"], filename=row["filename"], status=row["status"])

@app.put("/tasks/{task_id}", response_model=TaskOut)
def put_task(task_id: str, payload: TaskCreateIn):
    # PUT 幂等：重复 PUT 同一内容，资源状态收敛到同一值
    put_counter["count"] += 1
    # 若不存在则创建，若存在则替换 filename（简化演示幂等语义）
    existing = store.get(task_id)
    if existing is None:
        store.create(task_id, payload.filename)
    else:
        store.update(task_id, filename=payload.filename)
    row = store.get(task_id)
    assert row is not None
    return TaskOut(id=row["id"], filename=row["filename"], status=row["status"])

@app.delete("/tasks/{task_id}", status_code=status.HTTP_204_NO_CONTENT)
def delete_task(task_id: str):
    # DELETE 幂等：重复删除同一资源，效果一致（资源不存在）
    row = store.get(task_id)
    if row is None:
        # 已不存在仍返回 204，体现“删除的幂等性”
        return Response(status_code=status.HTTP_204_NO_CONTENT)
    store.delete(task_id)
    return Response(status_code=status.HTTP_204_NO_CONTENT)

@app.get("/healthz")
def healthz():
    # 演示 Header 的内容协商：返回 JSON，调用方可通过 Accept 校验
    return {"status": "ok"}

# --- 本地验证：用 TestClient 演示 HTTP 语义（无网络） ---
client = TestClient(app)

# 1) POST 创建 → 201 + Location
r1 = client.post("/tasks", json={"task_id": "t1", "filename": "meeting.wav"})
print("POST t1:", r1.status_code, r1.headers.get("location"), r1.json())
assert r1.status_code == 201
assert r1.headers["location"] == "/tasks/t1"

# 2) POST 重复 → 409（非幂等的体现：重复创建是冲突而非覆盖）
r2 = client.post("/tasks", json={"task_id": "t1", "filename": "meeting.wav"})
print("POST duplicate:", r2.status_code, r2.json()["detail"])
assert r2.status_code == 409

# 3) GET 存在 → 200；GET 不存在 → 404
r3 = client.get("/tasks/t1")
print("GET t1:", r3.status_code, r3.json())
assert r3.status_code == 200
r4 = client.get("/tasks/missing")
print("GET missing:", r4.status_code)
assert r4.status_code == 404

# 4) PUT 幂等：两次 PUT 同一内容，最终状态一致（均为 demo2.wav）
r5 = client.put("/tasks/t1", json={"task_id": "t1", "filename": "demo2.wav"})
r6 = client.put("/tasks/t1", json={"task_id": "t1", "filename": "demo2.wav"})
print("PUT twice:", r5.json(), r6.json())
assert r5.json()["filename"] == "demo2.wav"
assert r6.json()["filename"] == "demo2.wav"
# 注意：put_counter 累加说明“执行次数”可变，但“资源状态”幂等收敛

# 5) DELETE 幂等：两次删除均为 204
r7 = client.delete("/tasks/t1")
r8 = client.delete("/tasks/t1")
print("DELETE twice:", r7.status_code, r8.status_code)
assert r7.status_code == 204
assert r8.status_code == 204

# 6) Header 演示：X-Request-Id 透传（需在路由中显式处理或中间件中统一处理）
r9 = client.get("/tasks/t2", headers={"X-Request-Id": "req-123"})
# t2 不存在 → 404，但 Header 已参与请求（可在中间件中记录）
print("X-Request-Id sent, status:", r9.status_code)
assert r9.status_code == 404  # t2 确实不存在

# 7) 校验失败 → 422（Pydantic 在边界拦截，未进入业务层）
r10 = client.post("/tasks", json={"task_id": "", "filename": "x.wav"})
print("empty task_id:", r10.status_code)
assert r10.status_code == 422

tmpdir.cleanup()
print("HTTP 语义校验通过：方法/幂等/状态码/Header 各司其职")
# 预期输出:
# POST t1: 201 /tasks/t1 {'id': 't1', ...}
# POST duplicate: 409 task t1 already exists
# GET t1: 200 {'id': 't1', ...}
# GET missing: 404
# PUT twice: {'id': 't1', 'filename': 'demo2.wav', ...} ...
# DELETE twice: 204 204
# X-Request-Id sent, status: 404
# empty task_id: 422
# HTTP 语义校验通过：方法/幂等/状态码/Header 各司其职
```

```bash
# 本地验证
.venv/bin/python -c "from fastapi.testclient import TestClient; print('TestClient ok')"
```

> **与后续章节的衔接**：本节的“状态码即责任划分”将在 [4.4 异常处理与全局响应](4.4_exception_global_response.md) 统一为信封格式；幂等性与重试的权衡将在 [第6章 并发模型](../../part2_backend_development/chapter06_concurrency_perf/index.md) 结合异步重试进一步展开。
