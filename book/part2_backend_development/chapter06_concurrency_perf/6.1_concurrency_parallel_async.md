---
kernelspec:
  name: book-venv
  display_name: Python 3 (book)
---

# 并发、并行与异步

> 学完本节，你能回答：并发与并行有何本质差异？异步与非阻塞是否是同一回事？I/O 密集与 CPU 密集任务为何要选不同的并发模型？

## 先动手：用耗时对比区分三种“同时做”

别先背定义，先看计时器。下面的 Demo 让同一组 50 毫秒 I/O 任务分别走串行、线程、线程池，耗时差异本身就是定义。

```{code-cell} ipython3
import time
import threading
import concurrent.futures

def io_task(task_id: int, delay: float = 0.05) -> str:
    time.sleep(delay)
    return f"task-{task_id}"

# 串行：3 个任务各 50 毫秒，约 150 毫秒
t0 = time.perf_counter()
serial_results = [io_task(i) for i in range(3)]
t_serial = time.perf_counter() - t0
print("serial results:", sorted(serial_results))
print(f"serial elapsed: {t_serial:.3f}s")
assert sorted(serial_results) == ["task-0", "task-1", "task-2"]
assert 0.12 < t_serial < 0.35

# 裸线程：3 线程并发，约 50 毫秒完成
results_mt = []
threads = []

def _worker(tid: int):
    r = io_task(tid)
    results_mt.append(r)

t0 = time.perf_counter()
for i in range(3):
    th = threading.Thread(target=_worker, args=(i,))
    threads.append(th)
    th.start()
for th in threads:
    th.join()
t_thread = time.perf_counter() - t0
print("threading results:", sorted(results_mt))
print(f"threading elapsed: {t_thread:.3f}s")
assert sorted(results_mt) == ["task-0", "task-1", "task-2"]
assert t_thread < t_serial

# 线程池：同样重叠等待
t0 = time.perf_counter()
with concurrent.futures.ThreadPoolExecutor(max_workers=3) as ex:
    futs = [ex.submit(io_task, i) for i in range(3)]
    pool_results = sorted([f.result() for f in concurrent.futures.as_completed(futs)])
t_pool = time.perf_counter() - t0
print("pool results:", pool_results)
print(f"pool elapsed: {t_pool:.3f}s")
assert pool_results == ["task-0", "task-1", "task-2"]
assert t_pool < t_serial

print("并发对照通过：串行显著慢于线程与线程池，等待被重叠了")
# 预期输出:
# serial elapsed: 0.15x s
# threading elapsed: 0.05x s
# pool elapsed: 0.05x s
# 并发对照通过：...
```

```{code-cell} ipython3
import asyncio
import time

async def async_io_task(task_id: int, delay: float = 0.05) -> str:
    await asyncio.sleep(delay)
    return f"task-{task_id}"

async def _run_async_demo():
    t0 = time.perf_counter()
    s0 = await async_io_task(0)
    s1 = await async_io_task(1)
    s2 = await async_io_task(2)
    t_serial = time.perf_counter() - t0
    serial = sorted([s0, s1, s2])
    print("async serial:", serial, f"elapsed {t_serial:.3f}s")
    assert serial == ["task-0", "task-1", "task-2"]
    assert 0.12 < t_serial < 0.35

    t0 = time.perf_counter()
    results = await asyncio.gather(
        async_io_task(0), async_io_task(1), async_io_task(2)
    )
    t_conc = time.perf_counter() - t0
    conc = sorted(results)
    print("async concurrent:", conc, f"elapsed {t_conc:.3f}s")
    assert conc == ["task-0", "task-1", "task-2"]
    assert t_conc < t_serial
    return t_serial, t_conc

t_serial, t_conc = await _run_async_demo()
print(f"asyncio 对照通过：串行 {t_serial:.3f}s 大于并发 {t_conc:.3f}s")
# 预期输出:
# async serial: ['task-0', ...] elapsed 0.15x s
# async concurrent: ['task-0', ...] elapsed 0.05x s
# asyncio 对照通过：...
```

观测小结：串行约 150 毫秒，并发约 50 毫秒，差异来自等待是否被重叠。这个重叠能力叫并发，能在多核上真正同时执行叫并行，用“发起后不等结果”的方式实现叫异步。再补两个正交维度，同步与异步关注调用方是否等结果，阻塞与非阻塞关注等待时线程能否做别的事。`asyncio` 的 `await` 就是异步加非阻塞的协作式等待。

## 从耗时反推：I/O 密集与 CPU 密集如何选型

刚才的重叠只对 I/O 密集有效。I/O 密集如网络请求与文件读写，瓶颈在等待，适合用线程或协程重叠。CPU 密集如大规模数值计算，瓶颈在计算，Python 多线程受限，适合用进程或下沉到释放锁的 C 扩展。`m2t/audio.py` 的 `load_audio` 属 I/O 密集，而 `resample_audio` 若走 `librosa` 则属 CPU 密集，选型前先识别瓶颈。

工具定位也可从耗时理解：`threading` 是内核线程抢占式，适合 I/O 密集共享内存的场景。`concurrent.futures` 是线程或进程池的高层封装，适合批量任务统一接口。`asyncio` 是单线程事件循环协作式，适合高并发 I/O 与长连接，但阻塞调用会拖垮循环。

```bash
.venv/bin/python -c "import threading, concurrent.futures; print('concurrency ok')"
```

> 与后续章节的衔接：本节的等待重叠将在 [6.3 异步编程核心](6.3_async_programming_core.md) 落到 `await` 的让出语义，在 [6.4 FastAPI 异步陷阱](6.4_fastapi_async_pitfalls.md) 中体现为阻塞调用为何会让出失效。I/O 与 CPU 的分型将在 [6.2 Python GIL](6.2_python_gil.md) 中用实测进一步验证。
