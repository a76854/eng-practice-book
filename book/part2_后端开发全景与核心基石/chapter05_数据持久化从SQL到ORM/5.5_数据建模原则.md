---
kernelspec:
  name: book-venv
  display_name: Python 3 (book)
---

# 数据建模原则

> 学完本节，你能回答：为什么“所有数据放一张表”很快就会失控？范式化在解决什么冗余？反范式化又在为什么代价买单？如何为 MeetingToText 的任务与片段做建模取舍？

## 建模的第一性原理：冗余 vs 一致性

数据建模是在“冗余”与“一致性”之间做权衡：冗余让查询快但更新难，一致性让更新易但查询慢。没有银弹，只有 trade-off。

- **范式化（Normalization）**：拆表、去重、靠外键关联，保证“同一事实只存一处”，更新时改一处即可。
- **反范式化（Denormalization）**：合表、冗余、预计算，让读查询一次命中，避免 `JOIN`。

类比通讯录：范式化是“联系人表 + 地址表”，搬家只改地址表一行；反范式化是“每条联系人都冗余一份地址”，查询快但搬家需改多行。

## 三范式速览

- **1NF**：列不可再分，行有主键。反例：`speakers = "Alice,Bob"` 应拆为多行或关联表。
- **2NF**：在 1NF 基础上，非主键列完全依赖主键（消除部分依赖）。反例：`(task_id, speaker_id)` 为主键的表中存 `task_filename`（仅依赖 `task_id`），应拆出 `tasks` 表。
- **3NF**：在 2NF 基础上，非主键列不传递依赖。反例：`tasks` 中存 `speaker_name` 且 `speaker_name` 依赖 `speaker_id`，应拆出 `speakers` 表。

> **选型中立性**：范式化不是“越高越好”。写多读少的配置表适合 3NF，读多写少的报表、搜索索引、缓存常需反范式化；关键是明确“谁为冗余的一致性买单”——是应用层双写、定时任务，还是接受短暂不一致。

## MeetingToText 的建模思考

- **任务 `tasks`**：`id / filename / status / created_at / full_text`（见 `m2t/store.py`）—— 单表即可满足当前查询模式（按 `id` 点查、按 `created_at` 排序）。
- **片段 `segments`**：若需说话人、时间戳、文本的细粒度查询与编辑，则拆为 `segments(task_id, start, end, speaker, text)`，`task_id` 外键关联 `tasks`。
- **何时反范式**：若“列出任务时总要显示片段数”，可在 `tasks` 冗余 `segment_count` 列，用触发器或应用层更新，以一次查询换多次 `COUNT(*)`。

## 可运行示例：两张表建模对比

文件 `book/part2_后端开发全景与核心基石/chapter05_数据持久化从SQL到ORM/demo_modeling.py`（用 `sqlite3` 对比规范化与冗余，无网络）：

```{code-cell} ipython3
# 文件 book/part2_后端开发全景与核心基石/chapter05_数据持久化从SQL到ORM/demo_modeling.py
import sqlite3

conn = sqlite3.connect(":memory:")
conn.row_factory = sqlite3.Row
conn.execute("PRAGMA foreign_keys = ON")

# 方案 A：反范式——单表冗余（查询快，更新需改多行）
conn.executescript("""
CREATE TABLE tasks_denorm (
    id TEXT PRIMARY KEY,
    filename TEXT NOT NULL,
    status TEXT NOT NULL,
    speaker TEXT NOT NULL,
    text TEXT NOT NULL
);
""")
# 同一任务 t1 有两段，若 filename 需改名，要改两行
conn.executemany("INSERT INTO tasks_denorm VALUES (?, ?, ?, ?, ?)", [
    ("t1", "meeting.wav", "completed", "Alice", "hello"),
    ("t1_dup", "meeting.wav", "completed", "Bob", "world"),  # 为演示冗余，id 刻意不同
])
# 反范式查询：无需 JOIN，一次命中
denorm_rows = conn.execute("SELECT * FROM tasks_denorm WHERE filename='meeting.wav'").fetchall()
print("denorm rows:", len(denorm_rows))
assert len(denorm_rows) == 2

# 方案 B：范式化——两表 + 外键（更新易，查询需 JOIN）
conn.executescript("""
CREATE TABLE tasks_norm (
    id TEXT PRIMARY KEY,
    filename TEXT NOT NULL,
    status TEXT NOT NULL
);
CREATE TABLE segments_norm (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id TEXT NOT NULL REFERENCES tasks_norm(id) ON DELETE CASCADE,
    speaker TEXT NOT NULL,
    text TEXT NOT NULL
);
CREATE INDEX idx_segments_task ON segments_norm(task_id);
""")
conn.execute("INSERT INTO tasks_norm VALUES (?, ?, ?)", ("t1", "meeting.wav", "completed"))
conn.executemany("INSERT INTO segments_norm (task_id, speaker, text) VALUES (?, ?, ?)", [
    ("t1", "Alice", "hello"),
    ("t1", "Bob", "world"),
])
conn.commit()

# 范式化查询：JOIN 才能拿到完整视图
norm_rows = conn.execute("""
SELECT t.filename, s.speaker, s.text
FROM tasks_norm t JOIN segments_norm s ON s.task_id = t.id
WHERE t.id = 't1' ORDER BY s.id
""").fetchall()
print("norm join:", [(r["speaker"], r["text"]) for r in norm_rows])
assert len(norm_rows) == 2

# 一致性对比：改文件名
# 反范式需改多行（易遗漏）
conn.execute("UPDATE tasks_denorm SET filename = ? WHERE filename = ?", ("renamed.wav", "meeting.wav"))
print("denorm after rename:", conn.execute("SELECT COUNT(*) FROM tasks_denorm WHERE filename='renamed.wav'").fetchone()[0])

# 范式化只改一行
conn.execute("UPDATE tasks_norm SET filename = ? WHERE id = ?", ("renamed.wav", "t1"))
conn.commit()
print("norm after rename:", conn.execute("SELECT filename FROM tasks_norm WHERE id='t1'").fetchone()[0])
assert conn.execute("SELECT filename FROM tasks_norm WHERE id='t1'").fetchone()[0] == "renamed.wav"
# segments 无需改动，天然一致

# 冗余的代价：统计“每任务片段数”
# 范式化需聚合
counts = conn.execute("""
SELECT t.id, COUNT(s.id) AS cnt FROM tasks_norm t LEFT JOIN segments_norm s ON s.task_id = t.id GROUP BY t.id
""").fetchall()
print("counts:", [(r["id"], r["cnt"]) for r in counts])
assert counts[0]["cnt"] == 2

# 反范式若需“片段数”可冗余一列 segment_count，但需应用层维护一致性
# 演示：若在 tasks_denorm 上加 segment_count，更新时需双写
conn.execute("ALTER TABLE tasks_denorm ADD COLUMN segment_count INTEGER DEFAULT 0")
# 模拟应用层双写遗漏：只插了行，未更新计数
conn.execute("INSERT INTO tasks_denorm (id, filename, status, speaker, text) VALUES (?, ?, ?, ?, ?)",
             ("t2", "other.wav", "pending", "Alice", "foo"))
# 此时 segment_count 仍为 0，需额外 UPDATE 才能一致——这就是反范式的维护成本
print("denorm segment_count (stale):", conn.execute("SELECT segment_count FROM tasks_denorm WHERE id='t2'").fetchone()[0])

# 外键约束：删除任务级联删除片段
conn.execute("DELETE FROM tasks_norm WHERE id='t1'")
print("segments after delete:", conn.execute("SELECT COUNT(*) FROM segments_norm").fetchone()[0])
assert conn.execute("SELECT COUNT(*) FROM segments_norm").fetchone()[0] == 0

conn.close()
print("建模校验通过：范式化保一致、反范式换速度，trade-off 清晰")
# 预期输出:
# denorm rows: 2
# norm join: [('Alice', 'hello'), ('Bob', 'world')]
# denorm after rename: 2
# norm after rename: renamed.wav
# counts: [('t1', 2)]
# denorm segment_count (stale): 0
# segments after delete: 0
# 建模校验通过：范式化保一致、反范式换速度，trade-off 清晰
```

```{code-cell} ipython3
# 文件 book/part2_后端开发全景与核心基石/chapter05_数据持久化从SQL到ORM/demo_modeling_query.py
# 补充：用 m2t/store.py 的真实 TaskStore 验证“单表模型”的查询模式
import tempfile, pathlib
from m2t.store import TaskStore

tmpdir = tempfile.TemporaryDirectory()
store = TaskStore(pathlib.Path(tmpdir.name) / "modeling.db")
# 模拟 MeetingToText 的写入：任务创建后更新状态与文本
store.create("t1", "a.wav")
store.create("t2", "b.wav")
store.update("t1", status="completed", full_text="hello world")
# 查询：按创建时间倒序列出（依赖 idx_tasks_created_at）
rows = store.list_tasks(limit=10)
print("store rows:", [(r["id"], r["status"], r["full_text"][:5]) for r in rows])
assert len(rows) == 2
assert rows[0]["id"] in ("t1", "t2")  # 按 created_at 倒序
# 点查
print("get t1:", store.get("t1")["status"], store.get("t1")["full_text"])
assert store.get("t1")["full_text"] == "hello world"
tmpdir.cleanup()
print("TaskStore 建模校验通过：单表 + 索引满足当前查询模式")
# 预期输出:
# store rows: [('t2', ...), ('t1', ...)] 或相反取决于写入时序
# get t1: completed hello world
# TaskStore 建模校验通过：单表 + 索引满足当前查询模式
```

```bash
# 查看表结构（macOS / Linux）
.venv/bin/python -c "import sqlite3; c=sqlite3.connect(':memory:'); c.execute('CREATE TABLE t(id TEXT PRIMARY KEY, x INT)'); print(list(c.execute('PRAGMA table_info(t)')))"
# Windows 需用
.venv\Scripts\python.exe -c "import sqlite3; c=sqlite3.connect(':memory:'); c.execute('CREATE TABLE t(id TEXT PRIMARY KEY, x INT)'); print(list(c.execute('PRAGMA table_info(t)')))"
```

> **与后续章节的衔接**：本节的“何时拆表”将在 [第6章 并发模型](../../chapter06_并发模型与性能工程/index.md) 结合并发写入讨论锁粒度，在 [第10章 健壮性](../../part4_现代工程进阶与交付/chapter10_健壮性与安全底线/index.md) 结合校验讨论约束与一致性保障。
