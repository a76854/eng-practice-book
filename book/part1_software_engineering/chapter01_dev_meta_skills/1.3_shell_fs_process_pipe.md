---
kernelspec:
  name: book-venv
  display_name: Python 3 (book)
---

# Shell、文件系统、进程与管道

> Shell 是工程师的“第二编辑器”。理解文件系统、进程与管道，不是为了背命令，而是为了获得一种可组合的文本思维——把复杂任务拆成可串联的小工具。

## 文件系统：一切皆路径

一个通用项目的 `data/` 分层本身就是领域建模，目录结构在表达“数据流向”：

```
data/
  uploads/      # 外部输入的原始文件（输入）
  temp/         # 处理过程中的临时产物（中间态）
  app.db        # 任务与设置的持久化（状态）
```

关键直觉：

- **路径是接口**：`Path("data/uploads/a.wav")` 在 Python 与 Shell 中指向同一资源。Linux 文件系统大小写敏感（`a.WAV` 与 `a.wav` 是两个文件），路径分隔符统一为 `/`。
- **文件是字节流**：`cat`、`less`、`head` 只是按不同窗口观察同一字节流，Python 的 `open(..., "rb")` 亦然。

```bash
# 列出目录
ls -lh data/uploads

# 递归查找音频文件
find data -name "*.wav" | head

# 查看路径（Linux 统一为 / 分隔）
python -c "from pathlib import Path; print(Path('a/b/c'))"
```

## 进程：程序的运行实例

每个 `python -m myapp.server` 或 `npm run dev` 都是一个进程，拥有独立的 PID、环境变量、打开的文件描述符。理解进程对调试至关重要：

- `ps aux | grep uvicorn` 查看进程是否存在。
- 环境变量是进程的“上下文”：`APP_DATA_DIR=./data python -m myapp.cli serve` 只影响该次启动的子进程，不会污染全局。
- 退出码是进程的“返回值”：`0` 表示成功，非 `0` 表示失败，`&&` 与 `||` 基于此做流程控制。

```bash
# 启动服务（通用示例；实际命令由项目定义）
python -m myapp.cli serve --port 8000 --log-level INFO

# 进程管理
ps aux | grep myapp
kill <PID>
```

## 管道：把大问题切成小工具

管道 `|` 的哲学是：**每个工具只做一件事，把文本流作为统一接口**。

```bash
# 统计项目中 Python 文件行数
find . -name "*.py" | xargs wc -l | sort -n | tail
```

管道的威力在于**可组合性**：`find` 产生路径流 → `xargs` 转为参数 → `wc -l` 统计 → `sort` 排序 → `tail` 取尾部。每个环节都可独立替换与测试。
