# MyST (Jupyter Book 2) build chain — mystmd via npm, Python kernel deps for execution
# Python >=3.12, Node 24, mystmd 1.x + ipykernel/jupyter-client for code-cell execution
nbclient==0.10.1
ipykernel==6.29.5
jupyter-client==8.9.1
jupyter-server==2.20.0
PyYAML==6.0.2
fastapi==0.141.1  # week07+ 后端章节运行依赖（FastAPI 入门 + TestClient 演示）
PyJWT==2.13.0  # chapter10 JWT 库调用（替代手写 HMAC）
